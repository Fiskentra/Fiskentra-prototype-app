# Map design QA — v0.25 beta (current)

Recheck, 2026-09-16: [MAP_RECHECK.md](MAP_RECHECK.md) records the newer `18809E97…` APK and scoped device evidence. The 200% navigation HUD now retains the arrow/distance above an expanded point sheet with a measured 98 px gap; closing restores the full maneuver/street. The stale Places marker after switching to My points was reproduced and removed. Older capture provenance below remains unchanged.

Review date: 2026-09-15. Recaptures through 23:11 have been independently reviewed, including the fixed 200% font state, filters, compact offline panel, search return flow and deduplicated backtrack HUD. The final 05/06/07 captures and bounds diagnostics resolve the previously observed overview-camera defect for these recorded scenarios. Visual source: the eight numbered mockups in `C:/Fiskentra/Map_codex_update/mockups/`, with the functional requirements in `Fiskentra_Map_Codex_TZ.md`. Current native Android captures are in `qa/map-beta/`; the historical v0.24 review below does not certify v0.25.

Build provenance supplied by the device-test owner: the final 23:09–23:11 captures of 05/06/07 use installed APK SHA-256 `5028A34117A2A03FE5B21EEC7005F614D7B7600BA734CE54FBA60AB78319C79D`, 57,756,347 bytes; `qa/map-beta/final-build.log` confirms a successful 40-second build. The 200% retest uses `3D0A746CF0F33221C28877187C36A1EC00CA36DF4CFE97D806D2F797D077930D` (41-second build). The normal-font 01/08 and real-road 03 captures were taken on the preceding `F1490AD6…` build; they must not be silently attributed to a single later capture run.

This independent review opened all eight current device screenshots. References 01, 02, 03, 06 and 08 were also reopened alongside the captures; references 04, 05 and 07 were supplied as the user's attached visual sources. Comparison excludes native Android status/navigation bars and considers app-owned content at the device width. Native system UI and actual map/provider attribution are preserved. No screenshots or live data were fabricated to match the illustrations.

## Eight required visual states

| # / reference | Current device evidence and actual state | Comparison / current disposition |
|---|---|---|
| 01 `01-catch-details.png` | `qa/map-beta/01-catch-final.png`, 02:04:16 +02:00. Actual saved Catch, real local test photo, species Test, 2.4 kg / 80 cm; selected marker fully above the sheet. | **Visual PASS for the displayed normal-font expanded card.** Photo/metrics are side by side; the fixed footer now visibly contains 11 m straight-line distance and Coordinates synced · details on this device above all three complete Navigate/Edit/Details buttons. Close, title/time, star and more control are visible. The previous footer P2 is resolved in the recapture. Selected/GPS markers overlap at the actual nearby positions, but neither is clipped by the sheet. Button execution, notes scrolling and 200% font are not proven by this image. |
| 02 `02-map-filters.png` | `qa/map-beta/02-map-filters.png`, 02:27:09 +02:00. All points time scope, all six types checked, Favorites only unchecked, 125 located results / 0 without location. | **Scoped visual PASS.** Reset filters is now fully visible beside Filter map; Apply, checkbox labels and states are complete. The former Reset P2 is resolved. Dropdown time scope and six type choices deliberately preserve the supported filters, differing from the mock's three chips/rows. The displayed total matches the 125 stored points; every filter combination is not exercised by one image. |
| 03 `03-camera-follow.png` | `qa/map-beta/03-camera-follow.png`, 02:19:19 +02:00, preceding F149 build. Actual Driving route: 14.6 km / 22 min, right turn in 258 m onto Alabastergatan, ETA 02:41 with no-live-traffic disclosure. | **Visual PASS for the displayed road-navigation state.** Blue route with white casing, complete GPS marker, readable compact maneuver, compass, selected Driving, Steps and muted voice control are visible. The prior tall repeated backtrack HUD is no longer the content of this file. Driving/right turn/actual street differ legitimately from Walking/left turn/Lakeside trail in the illustration. The static capture does not prove travel, arrival, rerouting or spoken guidance. The separate `camera-free-after-gps.json` remains evidence from the earlier backtrack/gesture scenario only. |
| 04 `04-gps-accuracy.png` | `qa/map-beta/04-gps-accuracy.png`, 02:31:39 +02:00, and `gps-disabled-ui.json`. Location was actually switched off; the header/banner say Location is turned off and the last position is gray. The recapture also shows the transient GPS signal lost toast. | **Scoped visual PASS for the disabled-GPS warning.** The retained last position and warning are truthful; the transient toast overlaps lower controls in this frame, so their full visibility is not established by this capture. This is **not** staged ±60m accuracy, a fresh geodesic 60m uncertainty ring, or a low-accuracy recording test. No ±60m visual/field PASS claimed. |
| 05 `05-offline-area.png` | `qa/map-beta/05-offline-area.png`, 23:09:08 +02:00, final 5028 build. Existing Saved area, Satellite, radius 2 km, 30.3 MiB. The earlier failure remains in `05-before-framing-fix.png`. | **Scoped visual PASS for saved-area framing and panel.** The complete cyan rectangle and all four corners are visible above the compact sheet. One handle, Change area/Manage, Map ready offline and Coverage at map center · Available offline are complete. The panel truthfully reports no saved route geometry and the internet requirement for new road routes/rerouting. The previous camera-framing and stale coverage-label defects are resolved in this recapture. This is not an airplane-mode tile test. |
| 06 `06-backtrack.png` | `qa/map-beta/06-backtrack.png`, 23:09:34 +02:00, final 5028 build. Actual archived trip: 867 m remaining on the recorded line; the unrecorded approach is about 14.8 km. | **Scoped visual PASS for the off-track overview and HUD.** The upper HUD contains only Approach not recorded; full distance/GPS context remains in the lower panel. The cyan recorded geometry and Start marker are visibly framed above the panel; diagnostics measure 177 px vertical bounds rather than the former tiny overview. Offline and Locate controls are separate. Stop guidance/Full track and the foreground-guidance limit are fully visible. No on-track walking, gap traversal or arrival PASS inferred. |
| 07 `07-place-search.png` | `qa/map-beta/07-place-preview.png`, 23:11:04, and `07-place-search.png`, 23:11:13 +02:00, final 5028 build. Query Gavle, Places selected, actual Gävle kommun/Gävleborg/Gävle results. The earlier failure remains in `07-before-framing-fix.png`. | **Scoped visual PASS for selected-place framing and preview/return flow.** The selected municipality marker stays visible above both collapsed-preview and expanded-results sheets. Its large administrative extent legitimately uses a regional overview; the final bounds diagnostics confirm the selected bounds fit the usable viewport at both panel heights. Back to results restores the actual list; lower rows continue in the sheet's scroll area. The preview/return UI dumps supplement the images. These captures do not reproduce the mock's lake, three fully displayed rows or fixed Show on map footer, and do not prove every result action. No saved point was created for this test. |
| 08 `08-outdoor-map.png` | `qa/map-beta/08-outdoor-map.png`, 02:04:03 +02:00. Bright map at regional scale, cluster 116, real GPS marker near the right viewport edge, Trip ready rather than an active recording. | **Visual PASS for the displayed idle Outdoor state.** The new navy Trip ready pill has clear contrast against map labels and sits between the tools and blue Point button without overlap. Layer/offline/recenter controls and bottom navigation remain visible. The viewport cuts a small part of the cluster at the right edge; this regional free-map state does not establish an incorrect following camera. No concrete new P1/P2 in the checked controls. Detailed shoreline track, active-trip/Pause, Outdoor badge and individual catch markers at trail zoom remain outside this capture's proof. |

## Visual findings and scope

- No new P1 was identified in the displayed controls. The 01 footer, 02 Reset placement, real-road 03 HUD, 05 panel, 06 HUD repetition and 200% accessibility P2s are visually resolved. The final 23:09–23:11 images and bounds logs also resolve the reported shared camera-framing P2 for Show area, Show on map and Full track in the exercised scenarios.
- The final 01 image confirms distance/status above fixed actions. The 02:27 filter image now confirms Reset beside the heading and all 125 points selected by the all-types scope.
- 05/07 panel changes and shared bounds/padding correction are owned by their implementation agents. This reviewer did not duplicate those changes. Preserved before-framing-fix images document the earlier defect; final 05/06/07 recaptures and the projected-bounds diagnostics document its scoped resolution.
- The current full-width sheets occupy more map area than the illustrations. Normal-font action/status clipping was corrected on 01 and visually rechecked at 02:04. The expanded 200% card's footer, full connection labels, star and navigation labels are rechecked at 02:24, and the paired screenshot confirms body scrolling. Compact sheets, TalkBack reading order and action callback execution need separate evidence.
- Real geography, regional zoom, stored catch species/photo/measurements, disconnected Flic, actual GPS accuracy, route length, off-track distance, downloaded bytes and result counts must remain truthful. They are not replaced with mock-only Pine Lake/Pike/sample numbers. The selected point's photo is actual local user data.
- 03 now shows a real Driving route, while 06 remains an off-track backtrack state, not proof of an on-track walk. 04 is GPS disabled, not an injected ±60 m fix. 08 is Trip ready, not active recording. These state differences prevent claiming pixel-identical or full state acceptance.

## Read-back verification evidence

200% font history: `qa/map-beta/font-200.png` and `font-200-ui.json` revealed the favorite star clipped at its bottom, Devices clipped on the right and the visual Flic status ellipsized, despite complete text in the accessibility dump. These were P2s. The star was replaced with a density-sized ImageButton; bottom labels now fit one line and complete Flic/GPS statuses stack when fontScale > 1.3.

**200% retest: scoped PASS.** `font-200-fixed.png` (02:24:24), `font-200-scrolled.png` (02:24:31) and `font-200-fixed-ui.json` were independently inspected. Flic disconnected and Accuracy ±4 m are complete on separate lines. Star is fully visible with Add to favorites description and a 144×144 px / 48 dp target. Map/Journal/Saved/Devices are complete and readable. Navigate/Edit/Details, full distance and full local/cloud status remain fixed and visible in both images while the title/photo body moves through the scroll viewport. The changed crop is scrolling rather than lost photo data. The UI dump confirms complete labels and enabled controls; it does not prove every action callback or TalkBack speech. Font was restored to 1.1 by the test owner. This reviewer did not use ADB.

- `qa/map-beta/weather-swipe.log`, final 23:11:54 run: 7 PASS — Map visible, both edge swipes open forecast, both forecast swipes return to Map, map-body drag keeps Map open, header weather button opens forecast.
- `qa/map-beta/bounds-backtrack-framing.log`: device-test owner preserved the already-read ADB output verbatim before the rolling buffer changed; the provenance header records this. Final offline bounds are 522×522 px at zoom 10.705; final backtrack bounds are approximately 127×177 px at zoom 12.551. Both record centerInside=true and cornersVisible=true. `bounds-framing.log` independently records the final search sequence, with true flags for collapsed and expanded panels; the collapsed-preview selection has approximately 282×403 px bounds at zoom 5.800. These numeric checks supplement the screenshots and do not prove every geometry, rotation or screen size.
- `qa/map-beta/lifecycle/report.json`:20/20 Map→Saved→Map cycles,40 UI-verified transitions over164.295s; same PID23451,1 Activity,1 ViewRoot and214 Views in start/middle/end observations. This is navigation-observation PASS, not retained-memory/GC proof or a timing benchmark.
- `qa/map-beta/regression-summary.md`:444 checks across latest JVM group logs. This does not replace runtime UI validation.
- `app/build/reports/lint-results-debug.xml`, independently reread with modification time 23:07:27: 0 errors / 100 warnings. The refreshed `qa/map-beta/lint-review.md` agrees with the final 40-second build and 5028 APK provenance and preserves the warning history. No blanket suppression and no runtime safety inference from lint.
- `qa/map-beta/runtime-defects.md`:original stale-Style crashes, implemented lifecycle guard, limited successful navigation retest and pending state-specific checks.
- `qa/map-beta/final-data-integrity.json`, 02:33 read-back: PASS — 125/125 legacy points, zero changes to ID/coordinates/time/type/note/title/symbol/color/size/weather/catch details, exact legacy backup, original photo SHA/link retained, 9/9 archived trips, 132/132 archived coordinates and 13/13 segment gaps unchanged. Current track remains 1/1 point. The sole changed legacy map preference is `route_profile`, reflecting the Driving test; this is not a point mutation. No test point was added or removed. This read-back predates the final evening capture run.

Current v0.25 visual result: **scoped PASS for the captured states**, with no unresolved material visual defect identified in this review. The displayed 01, 02, real-road 03, disabled-GPS 04, saved-area 05, off-track 06, search 07, idle 08 and the 200% expanded-card state have the limited evidence described above. The final overview-camera recaptures pass. Full beta physical acceptance, TalkBack speech and remaining state-specific interactions are incomplete; final performance results are documented separately by their test owner. No full v0.25 acceptance or pixel-identical match is asserted. Actual device states for 04 and 08 remain different from the illustrative mock states.

---

# Navigation design QA — v0.24 (historical)

Source visual truth: `qa/map-v024/reference.png`, copied from `C:/Users/Gebruiker/Downloads/Codex Image Sep 14, 2026, 01_48_56 AM.png`.

Implementation first capture: `qa/map-v024/driving-first.png` (actually Walking, the user's saved profile). Both images were opened together in the same tool comparison input.

Viewport: source 845×1860, device 1080×2376 at Android density 3 (360 dp wide). Compare app content after excluding native status/navigation bars; source proportions are normalized to the 360 dp content width (845 px → 360 dp). The device has larger system font scaling. This is a native Android screen, not a browser or a scaffolded web prototype.

State: active route, satellite layer. Real device geography, right turn, connected Bluetooth and actual trip data differ from the illustrative left-turn/parking mock. Existing Fiskentra symbol + wordmark and weather access are intentionally retained from the user's prior instructions. Parking and a verified final walking leg are not invented for an arbitrary route endpoint.

## Comparison history

First comparison:
- [P2] Guidance panel too wide/left aligned relative to source. Fix: cap its width at 224 dp, align it next to the compass, and allow smaller screens to constrain it.
- [P2] Maneuver glyph thinner than target. Fix: weight the original Material Icons path with a 1-unit round stroke; do not replace it with a font arrow.
- [P2] Unnamed path repeated `Turn right` beneath `Turn right`. Fix: use actual provider street names or explicit `Unnamed path` / `Unnamed road`.
- Compact mode row, outlined/filled travel modes, time/distance row and blue/navy colors are present. Steps opens the existing real maneuver list. Walking/Driving switching verified.

## Required fidelity surfaces

- Typography: system sans-serif, bold 18 sp maneuver and 20 sp route total, quieter 11/12 sp distance and street. Long streets may wrap over two lines. System font settings retained.
- Spacing: compact two-row lower panel, divider, separate maneuver panel next to compass, unchanged bottom app navigation; map receives the freed space.
- Colors: navy #001e2e, cyan #00a7ff, active blue #0059e8, muted #b7c9e6; matches the reference palette.
- Assets: live MapLibre satellite map and original Fiskentra brand asset; official Material Icons converted mechanically into Android vectors. Existing app icons reused for Steps and audio. No raster map or fabricated POIs.
- Content: live distance, ETA, maneuver, street and route endpoint gap. No live-traffic claim. Voice is opt-in, engine-dependent and foreground only.

Post-fix comparison: `qa/map-v024/driving-refined.png` opened together with the original reference in the same image comparison input. Active Driving now displays the real street on its own line, the heavier direction arrow and narrower guidance card. The source's left turn and shorter sample journey differ intentionally from the live right turn / 24.7 km route. No remaining actionable P0/P1/P2 visual findings in the requested navigation controls.

Focused regions: the guidance and footer were checked in the readable full-resolution pair (distance/action/street hierarchy, arrow weight, divider, selected modes, audio icon and ETA all legible); no additional cropped artifact was needed.

Interactions: Walking route displayed; Driving selected and real Driving Steps opened; voice toggle entered enabled state with the available engine, then muted. Route restoration after update verified. Foreground-only speech is additionally enforced by the activity lifecycle. Actual audible instructions and road travel remain physical test gaps.

P3/accepted differences: accessibility font sizing makes the guidance card slightly wider and route summary taller than the mock; real endpoint-gap text needs a second line. Native compass follows actual heading; existing clustered markers reflect the user's point density. Original brand lockup/weather controls deliberately retained. Final images keep native system bars, which are excluded from content comparison.

Implementation checklist: all three initial visual findings fixed and compared again; final build/lint and 63 relevant JVM checks pass. All seven `qa/Test-WeatherSwipe.ps1` checks pass with the new navigation UI. Final APK installed successfully on OPPO CPH2609 without resetting app data.

Historical v0.24 final result: passed
